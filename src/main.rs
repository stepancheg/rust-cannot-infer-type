extern crate env_logger;

pub struct Payload {
    pub xxx: ::std::vec::Vec<u8>,
}

impl Payload {
    fn compute_size(&self) {
        if self.xxx != ::std::vec::Vec::new() {
        };
    }
}

fn main() {
}
